[project]
name = "jobhunter"
version = "0.1.0"
description = "End-to-end AI-powered job hunting agentic system"
requires-python = ">=3.11"
dependencies = [
    "google-genai>=1.0.0",
    "langgraph>=0.2.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "python-dotenv>=1.0.0",
    "PyGithub>=2.1.1",
    "playwright>=1.40.0",
    "httpx>=0.27.0",
    "rich>=13.0.0",
    "tenacity>=8.2.0",
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.29.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.3.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
asyncio_mode = "auto"
